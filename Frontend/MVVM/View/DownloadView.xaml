<UserControl x:Class="Frontend.MVVM.View.DownloadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Frontend.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border VerticalAlignment="Stretch" HorizontalAlignment="Left" Width="350" Background="#A27FB8" Margin="20" CornerRadius="70">
            <StackPanel HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">

                <TextBox x:Name="TextBox_SongUrl" Margin="0,20,0,0" TextChanged="TextBox_TextChanged" Tag="Song URL"/>
                <TextBox x:Name="TextBox_SongName" Margin="0,20,0,0" TextChanged="TextBox_TextChanged_1" Tag="Song Name"/>
                <TextBox x:Name="TextBox_Artist" Margin="0,20,0,0" TextChanged="TextBox_TextChanged_2" Tag="Artist"/>
                <Button Margin="10" Height="Auto" Content="Download" Click="Button_Click"/>
                <ProgressBar Value="24" Height="20" Cursor="Wait"/>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>

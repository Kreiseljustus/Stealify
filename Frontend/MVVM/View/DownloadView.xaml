<UserControl x:Class="Frontend.MVVM.View.DownloadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Frontend.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border VerticalAlignment="Stretch" HorizontalAlignment="Left" Width="350" Background="#A27FB8" Margin="20" CornerRadius="70">
            <StackPanel HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">

                <TextBox x:Name="TextBox_SongUrl" Style="{StaticResource DownloadTextBox}" Margin="0,20,0,0" Text="Song url" TextChanged="TextBox_TextChanged"/>
                <TextBox x:Name="TextBox_SongName" Style="{StaticResource DownloadTextBox}" Margin="0,20,0,0" Text="Song name" TextChanged="TextBox_TextChanged_1"/>
                <TextBox x:Name="TextBox_Artist" Style="{StaticResource DownloadTextBox}" Margin="0,20,0,0" Text="Artist" TextChanged="TextBox_TextChanged_2"/>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
